// Todo: what is this?

.entry
  .heading

    %h3
      %a{:href => blog_post_path(post)}
        = post.title
      - if post.draft
        %span.label.label-warning Draft post (not publicly visible)

    %h4
      %span.date
      = link_to_blog_post_author( post )
      %span.date
        on
        = format_date( post.created_at )
  = render :partial => "post_stats", :locals => {:post => post}
  .clearfix

