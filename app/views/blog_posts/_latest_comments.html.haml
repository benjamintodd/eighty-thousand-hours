%h3= link_to 'Latest blog comments', blog_posts_path
- Comment.blog.order("created_at DESC").limit(4).each do |comment|
  = render partial: 'shared/sidebar_item',
           locals: { date: comment.created_at, 
                     content: link_to_comment_contents( comment, 80 ), 
                     attribution: link_to_comment_author( comment ) }
