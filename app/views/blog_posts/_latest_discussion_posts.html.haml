%h3= link_to 'Latest forum posts', discussion_posts_path
- DiscussionPost.published.recent(4).each do |post|
  = render partial: 'shared/sidebar_item',
           locals: { date: post.created_at, 
             content: "#{link_to post.title, discussion_post_path( post )}".html_safe,
             attribution: link_to_discussion_post_author( post )}
