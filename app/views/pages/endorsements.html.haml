- @endorsements.each do |e|
  .endorsement
    .body
      = markdown e["body"]
    .attribution
      .author
        = e["author"]
      - if !e["position"].nil?
        .position
          = e["position"]

